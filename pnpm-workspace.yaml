allowBuilds:
  esbuild: true
  '@parcel/watcher': true
  unrs-resolver: true
  # Puppeteer's postinstall script downloads the browser binary used by the app.
  puppeteer: true

publicHoistPattern:
  - '@inertiajs/core'

overrides:
  'minimatch@10>brace-expansion': 5.0.9
